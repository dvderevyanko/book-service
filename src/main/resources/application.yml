spring:
  datasource:
    url: jdbc:postgresql://localhost:6666/book_db
    driverClassName: org.postgresql.Driver
    username: postgres
    password: postgres
  liquibase:
    change-log: classpath:/db/changelog.xml
    enabled: true